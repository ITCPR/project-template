## Data Directory

This directory stores all data used or generated by the project.

### Subdirectories
- `raw/` – original, immutable data
- `processed/` – derived data generated via scripts
- `external/` – symlinks or references to external datasets

### Policy
- Raw data must never be modified
- Processed data must be reproducible
- Large datasets should not be committed directly
